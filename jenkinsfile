pipeline {
 agent { docker {image 'node:6.3' } }
 stages {
  stage('build') {
   steps {
    sh '''
        npm --version
        echo " > install dependencies"
        yarn install
        echo " > running tests"
        pwd
       '''


   }
  }
 }
}